import SealCheckboxGroup from '../checkbox/src/checkbox-group.vue';

/* istanbul ignore next */
SealCheckboxGroup.install = function(Vue) {
  Vue.component(SealCheckboxGroup.name, SealCheckboxGroup);
};

export default SealCheckboxGroup;
