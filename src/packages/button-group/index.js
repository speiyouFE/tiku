import SealButtonGroup from '../button/src/button-group';

/* istanbul ignore next */
SealButtonGroup.install = function(Vue) {
  Vue.component(SealButtonGroup.name, SealButtonGroup);
};

export default SealButtonGroup;

